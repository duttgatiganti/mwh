apiVersion: admissionregistration.k8s.io/v1
kind: MutatingWebhookConfiguration
metadata:
  name: mutating-webhook
webhooks:
  - name: pod-labeler.k8s.io
    clientConfig:
      service:
        name: pod-labeler-service
        namespace: default
        path: /mutate
      caBundle: <base64-encoded-ca-cert>  # Replace with the base64-encoded certificate
    rules:
      - operations: ["CREATE"]
        apiGroups: [""]
        apiVersions: ["v1"]
        resources: ["pods"]
    admissionReviewVersions: ["v1"]
    sideEffects: None

